# 법학 문제집 PDF → Excel 변환 자동화

## 프로젝트 목표
법학 객관식 문제집 PDF를 구조화된 Excel 파일로 자동 변환합니다.
- **입력**: 법학 문제집 PDF 파일
- **출력**: Excel (전체 문제 / 검토 필요 / 단원별 / 통계)
- **자동화 수준**: PDF 지정 후 완전 자동 (v6)

## v6 핵심: Claude Code CLI 서브프로세스 자동 호출

기존 v5의 "Claude Pro 웹에 수동 붙여넣기" 방식을 `claude -p` 헤드리스 호출로 자동화했습니다.
따라서 사용자의 개입은 **PDF 파일 지정 1회만** 필요합니다.

---

## 텍스트 변환 규칙 (Claude Pro Project Knowledge 통합)

### 역할
PDF에서 추출된 법학 문제 텍스트의 OCR 오류를 보정하고 JSON으로 구조화합니다.

### 핵심 원칙

1. **원문 우선**
   - 명백한 OCR 오류만 수정 (한자·영문이 섞여 있거나 맥락상 분명히 틀린 경우)
   - 불확실하면 `[의심:추정값]` 태그를 붙여 표시
   - 원본 형식(띄어쓰기, 줄바꿈 패턴)을 최대한 유지

2. **법률 용어 보정**
   - 법학 문맥에 맞게 용어 교정 (예: "게약" → "계약")
   - 다만 보정 후에도 사전에 없는 용어는 `[의심:...]`으로 플래깅

3. **JSON 출력 (고정 형식)**
   - 다음 형식으로만 응답 (다른 텍스트/설명 금지)
   ```json
   {
     "questions": [
       {
         "id": 1,
         "text": "문제 내용 (첫 선지에만 전체 문제, 이후 선지에는 '동일')",
         "options": [
           {
             "num": "①",
             "text": "선지 내용",
             "correct": true,
             "explanation": "해설"
           }
         ]
       }
     ]
   }
   ```

4. **완전성**
   - 모든 선지(①②③④⑤ 등)를 누락 없이 포함
   - 해설이 없으면 빈 문자열 `""` 사용
   - 문제 번호 누락 없음

---

## 자주 등장하는 법률 용어 (사전 보정 대상)

| 자주 틀리는 패턴 | 정확한 용어 |
|---|---|
| 게약, 계약 → | **계약** |
| 청악, 청약 → | **청약** |
| 민법총칙 | 민법총칙 |
| 물권, 뭴권 → | **물권** |
| 채권, 채권, 채김 → | **채권** |
| 담보권 | 담보권 |
| 특수한물권 | 특수한 물권 |
| 소유권 | 소유권 |

※ 프로젝트 `dictionaries/legal_terms.json`에서 확장 가능

---

## 파일명 규칙

- **입력**: `input/문제집_1.pdf`, `input/강의자료_2월.pdf` 등
- **출력**: `chunks/texts/chunk_001.txt`, `chunks/hints/chunk_001_hint.json` 등
- **최종**: `output/최종_문제집.xlsx` (통합 파일)

---

## 다음 단계 (v6 개발 계획)

- [x] Phase 0: 프리플라이트 (CLI 설치/인증/인코딩 사전 점검)
- [x] Phase 1: PDF 전처리 + 동적 청킹
- [x] Phase 2: Claude CLI 서브프로세스 자동 호출 (v6 핵심)
- [x] Phase 3: 검증 및 신뢰도 점수
- [x] Phase 4: Excel 생성 (전체 문제 / 검토 필요 / 변환 통계 3개 시트 — "단원별" 시트는 미구현)

---

## 기술 사항

- **CLI 라이브러리**: Claude Code CLI (`claude -p` 헤드리스 모드)
- **인코딩**: UTF-8 (모든 입출력 경계에서 명시)
- **프롬프트 전달**: 임시 파일 경유 (Windows 쉘 이스케이프 회피)
- **권한 제한**: `--allowedTools` 설정으로 최소 권한 원칙 준수
- **타임아웃**: 청크당 300초 (config 조정 가능)
