# 법학 문제집 PDF → Excel 변환 자동화 (v6)

# PDF 처리
PyMuPDF==1.24.1

# OCR 재추출 (스캔 PDF의 임베디드 텍스트 레이어 품질이 낮을 때 사용,
# config.yaml의 pdf.extract_method: auto/ocr 에서 필요.
# 시스템에 Tesseract OCR 엔진 + 한국어 언어팩도 별도 설치 필요:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki (Korean 언어팩 체크)
#   Mac: brew install tesseract tesseract-lang
#   Linux: apt install tesseract-ocr tesseract-ocr-kor
pytesseract==0.3.10
Pillow==10.4.0

# Excel 생성
openpyxl==3.11.5

# 설정 파일 처리
pyyaml==6.0.1

# JSON 검증
jsonschema==4.20.0

# 텍스트 처리
# (difflib, re 등은 Python 표준 라이브러리 - 별도 설치 불필요)

# Logging 및 유틸
python-dotenv==1.0.0

# (선택) 진행률 표시
tqdm==4.66.1

# (선택) CLI 테스트
pytest==7.4.3
pytest-cov==4.1.0
